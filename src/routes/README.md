# Routes

This folder contains the application routes used by TanStack Router.

| File          | URL              |
| ------------- | ---------------- |
| `index.tsx`   | `/`              |
| `contact.tsx` | `/contact`       |
| `__root.tsx`  | shared app shell |

After adding a route, update `src/routeTree.gen.ts` to register it with the router.
